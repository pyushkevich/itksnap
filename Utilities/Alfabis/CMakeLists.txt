FIND_PACKAGE(CURL)

IF(CURL_FOUND)
  INCLUDE_DIRECTORIES(${CURL_INCLUDE_DIR})
ENDIF(CURL_FOUND)

ADD_EXECUTABLE(alfabis_client AlfabisCLI.cxx)
TARGET_LINK_LIBRARIES(alfabis_client ${CURL_LIBRARIES} ${ITK_LIBRARIES})

